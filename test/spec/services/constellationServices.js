'use strict';

describe('Service: constellationServices', function () {

  // load the service's module
  beforeEach(module('constellationApp'));

  // instantiate service
  var constellationServices;
  beforeEach(inject(function(_constellationServices_) {
    constellationServices = _constellationServices_;
  }));

  //it('should do something', function () {
  //  expect(!!constellationServices).toBe(true);
  //});

});
